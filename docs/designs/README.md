# Auto Translation Core 设计文档索引

> 本目录是项目设计决策的唯一索引入口。重构、评审或续作前，先从本页确认文档状态、适用范围和覆盖关系，不得仅凭文件名或旧实现判断当前产品逻辑。

## 1. 文档状态

| 状态 | 含义 |
|---|---|
| 当前基准 | 已由产品讨论确认，是对应范围内实施与重构的首要依据 |
| 专项基准 | 对某个独立子系统仍然有效，但不得覆盖更高优先级的当前基准 |
| 部分有效 | 仍包含可复用设计，但部分章节已被新文档取代 |
| 历史背景 | 用于解释既有代码和约束，不直接定义新功能 |
| 草案 | 尚有关键决策未确认，不得作为最终产品约束 |
| 已废弃 | 不再用于实施，只保留历史记录 |

## 2. 当前设计清单

| 文档 ID | 状态 | 文档 | 适用范围 | 阅读优先级 |
|---|---|---|---|---:|
| ATC-DES-003 | 当前基准 | [五步独立翻译工作流与数据架构](./v4-five-stage-independent-translation-workflow.md) | XML/DLL 分析、试跑、AI 分类复核、AI 翻译、单一裁决优先级、状态模型、持久化与重构边界 | 1 |
| ATC-DES-002 | 专项基准 | [Policy 云端判断加速契约](./v4-policy-analysis-cloud-contract.md) | Cloudflare Worker、D1、云端查询与贡献协议 | 2 |
| ATC-DES-001 | 专项基准 | [V4 DLL UI、Policy、API 与术语统一实施计划](./v4-dll-ui-policy-and-terminology-implementation-plan.md) | 一键翻译编排、DLL 静态分析、编辑器、API 生命周期、术语系统及既有实现背景 | 3 |
| ATC-DES-000 | 历史背景 | [项目设计说明](../../DESIGN_NOTES.md) | XML 翻译、全局翻译记忆、翻译包、热重载和兼容性底线 | 4 |

辅助工程资料：

- [项目结构](../../PROJECT_STRUCTURE.md)：源码、项目文件、构建输出和 Mod 包目录约定。
- [硬编码 UI 候选报告](../reports/drop-pod-jammer-hardcoded-ui-candidates.md)：特定样本的静态分析记录，不是通用产品设计。

## 3. 文档覆盖关系

当文档内容冲突时，按以下规则处理：

1. 范围更具体且状态为“当前基准”的文档优先。
2. 同一范围内，文档明确写出的“取代/废弃”关系优先。
3. 仍无法判断时，以最近一次由用户确认并登记到本索引的决策为准。
4. 当前代码行为不自动等于当前产品设计；重构时必须区分“现状”和“目标”。

当前已确认的覆盖关系：

- `ATC-DES-003` 细化 `ATC-DES-001` 的内部步骤职责、独立入口、单一裁决优先级、状态模型和持久化边界，不废弃一键翻译。一键翻译作为显式编排器调用步骤 1、2、4、5，步骤 3 仍是独立的成本预估入口。
- `ATC-DES-001` 的一键翻译方向、DLL 候选召回、静态 IL 分析、编辑器、API 生命周期、错误诊断和术语设计继续有效；涉及具体步骤输入输出时以 `ATC-DES-003` 的职责边界为准。
- `ATC-DES-002` 的云端协议仍有效，但客户端触发时机必须服从 `ATC-DES-003`：云端或 AI 分类复核不得暗中重新执行 XML/DLL 分析。
- `ATC-DES-000` 的安全、兼容性、翻译包输出和热重载约束继续有效，但其中旧的线性翻译流程不再定义新版工作流入口。

## 4. 重构阅读顺序

后续重构当前项目时，按以下顺序阅读：

1. `ATC-DES-003`：先建立工作流步骤、状态和持久化边界。
2. `ATC-DES-000`：确认不能破坏的既有核心能力。
3. `ATC-DES-001`：落实一键编排以及 DLL、API、编辑器和术语实现要求。
4. `ATC-DES-002`：需要接入云端判断缓存时再实施协议。
5. 对照当前源码制作“保留、迁移、替换、删除”清单，不能直接把旧流水线改名后继续使用。

## 5. 新增或修改设计文档的登记规则

每份设计文档开头必须至少包含：

```text
文档 ID
状态
最后更新日期
适用范围
取代/依赖关系
尚待确认事项
```

新增设计文档时必须同步完成：

1. 在本索引登记文档 ID、状态、范围和阅读优先级。
2. 在新旧文档中双向注明覆盖或依赖关系。
3. 将未确认事项集中列出，不得混写成已经确认的需求。
4. 决策变化时更新原文和索引，不另建内容重复但关系不明的文档。
5. 实施状态与产品设计状态分开记录；“代码已存在”不等于“产品设计仍有效”。

文档 ID 按 `ATC-DES-xxx` 递增，已有 ID 不得复用或改给其他文档。
